@using Microsoft.AspNetCore.Identity
@using SmartPark.Models

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<ul class="navbar-user ms-auto">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item user-greeting">
            <a id="manage" class="nav-link user-pill" 
               asp-area="Identity" asp-page="/Account/Manage/Index">
                @UserManager.GetUserName(User)
            </a>
        </li>

        <li class="nav-item">
            <form id="logoutForm" class="d-inline" asp-area="Identity" asp-page="/Account/Logout"
                  asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button id="logout" type="submit" class="btn logout-pill">
                    Logout
                </button>
            </form>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link auth-pill" id="register" asp-area="Identity" asp-page="/Account/Register">
                ✍️ Register
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link auth-pill" id="login" asp-area="Identity" asp-page="/Account/Login">
                🔑 Login
            </a>
        </li>
    }
</ul>
